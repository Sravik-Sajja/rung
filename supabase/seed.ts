// Repeatable canonical-demo seed entry point; it will upsert curriculum, roster, and cached outputs.
// TODO: use the server-only Supabase client to upsert the canonical demo dataset.
// This separate entry point is intentionally in place before persistence is built.
console.log("Rung seed skeleton: add canonical Supabase upserts here.");
